{
  "name": "conduit-e2e-playwright",
  "version": "1.0.0",
  "private": true,
  "description": "Projeto de automação de testes end-to-end da aplicação Conduit utilizando Playwright e TypeScript.",
  "license": "MIT",
  "scripts": {
    "dev": "concurrently --names \"Node,Vite\" -c \"green,blue\" \"npm run dev -w backend\" \"npm run dev -w frontend\"",
    "start": "npm run build -w frontend && npm run start -w backend",
    "test": "playwright test",
    "test:e2e": "playwright test",
    "test:headed": "playwright test --headed",
    "test:ui": "playwright test --ui",
    "test:debug": "playwright test --debug",
    "test:report": "playwright show-report",
    "test:unit": "vitest",
    "typecheck": "tsc --noEmit"
  },
  "workspaces": [
    "backend",
    "frontend"
  ],
  "devDependencies": {
    "@playwright/test": "^1.60.0",
    "@types/node": "^25.9.1",
    "@vitejs/plugin-react-swc": "^4.2.3",
    "concurrently": "^9.2.1",
    "vitest": "^4.0.18"
  },
  "dependencies": {
    "mysql2": "^3.22.3"
  }
}